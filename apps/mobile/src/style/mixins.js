import { isIOS } from 'util/platform'

export default {
  allCentered: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center'
  },

  navigationText: {
    color: 'white',
    fontSize: 18,
    lineHeight: 42
  },
}
