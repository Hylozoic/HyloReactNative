import { StyleSheet } from 'react-native'

const streamMargin = 0

export default StyleSheet.create({
  postRow: {
    paddingBottom: 12,
    marginRight: streamMargin,
    marginLeft: streamMargin
  }
})
