import styles from '../SignupFlow.styles'

export default {
  ...styles,
  container: {
    ...styles.container,
    justifyContent: 'space-between'
  },
  continueButton: {
    ...styles.continueButton,
    marginBottom: 20
  }
}
